# Needs more work

Incomplete/research-only patches live here. Do not use them as flash candidates.

Address correction alone is not completion. Any promoted replacement needs exact target identity, original-byte guards, a deterministic builder, independent verifier, reverse path and the relevant validation gates.