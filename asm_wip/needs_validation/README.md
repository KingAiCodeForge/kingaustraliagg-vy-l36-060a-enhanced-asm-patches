# Needs validation

These files may contain useful hardware-output ideas, but they are quarantined.

Promotion requires exact register ownership, scheduler/ISR context, masking/sibling-bit behavior, fault/fallback handling and a physical output waveform on bench hardware. New TIO research is not blanket approval of these older guesses.