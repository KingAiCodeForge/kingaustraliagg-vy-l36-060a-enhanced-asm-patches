# Rejected methods

Files here are negative evidence. Keep them so the same approach is not rediscovered and repeated.

A rejected method is not promoted by changing addresses, RAM bytes or bank placement. Re-opening it requires new evidence that addresses the reason for rejection.